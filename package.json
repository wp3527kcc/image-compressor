{
  "name": "image-compressor",
  "version": "2.0.0",
  "description": "图片和视频压缩工具 - 支持阿里云 OSS 存储",
  "scripts": {
    "dev": "node dev-server.js",
    "start": "node dev-server.js",
    "deploy": "vercel",
    "migrate:auth": "node -e \"require('dotenv/config'); require('./api/db').ensureAuthSchema().then(()=>console.log('auth schema ready'))\""
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "ali-oss": "^6.23.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.3",
    "nodemailer": "^9.0.1",
    "sharp": "^0.33.5"
  }
}